import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Streamline Your Social Media Workflow with Identable",
  description:
    "Streamline your social media workflow with Identable's intelligent content optimization and automated scheduling.",
  alternates: {
    canonical: "https://dindin.dev/",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
